# Accessibility and ARIA attributes

- Use semantic HTML elements (e.g., <button>, <nav>, <main>, <form>, <label>, <input>).
- Add aria-label, aria-live, and role attributes where appropriate.
- Ensure all interactive elements are keyboard accessible.
- Use color contrast tools to check readability.
- Run `npm run lint` to check for accessibility issues.
